# Current TAR guard rewrite diagnostic, 2026-09-25

**Inconclusive for promotion on this host; retain the optional candidate for a quiet-host qualification.** The exact current `0005-rust-tar-checksum.patch` produced a consistent complete-task CPU reduction across two fresh runs on one accepted CPython 3.16 executable. The host carried load averages around 4 throughout, and same-side wall drift reached 8.2%, so these runs are a bounded diagnostic, not a publishable speed claim. The older 9–12% results came from earlier source revisions and are not evidence for this current guard.

The accepted, read-only interpreter was `/Users/josh/d/python-build/rust-cpython/stage/bin/python3.16`, SHA-256 `6eeeb64b9eb161d4d5edb9200e79146dabd8acc0f73fcdca48417d0e3c9531bd`. Its `tarfile.py` matched the pure pinned source SHA-256 `a1a9f6ed36eb63b24a1bfb2c70210c2f42251559e796968183e90202b34bd44a`. An APFS clone kept the same executable and libpython bytes and received the exact current TAR hunk, producing `tarfile.py` SHA-256 `d008a71d8ca02d773befae4db3df875eba245d432b8771e1aa981e6c913a27b3`. The C and Rust new-file hunks were extracted from that same patch and built into `_rust_tar_checksum.cpython-316-darwin.so`, SHA-256 `cffd05fdbee3dcd9133de3dbb033922fccb1c74066f017af02e9dc652a39f227`. A direct probe confirmed lazy import and loading of this extension after an eligible checksum. The patch SHA-256 was `0da95722ef14a790c984c7056929c7d08eaa7deb7a21e6a3b64df4296ce42df6`.

The exact compiler argument vectors, resolved nightly Rust 2026-09-15 `rustc` path, LLVM 23.1.2 `clang` path, SDK, environment, source hashes, and each build resource record are in [run c data](data/tar-guard-measure-20260925-c.json). The build used direct `rustc` rather than the `rustup` launcher, then compiled `module.c` and linked the bundle with the accepted stage's headers. Each run's three commands took about 0.24 s combined wall and 0.23–0.24 s combined direct user plus system CPU; peak direct-process RSS was about 96 MB. `os.wait4` records each direct compiler process. The linker invoked by clang is a possible short-lived child that this accounting does not separately enumerate, so build totals are a lower bound for a complete compiler process tree. The benchmarked Python workload does not spawn children.

The [complete rewrite workload](source_tar_rewrite.py) streamed the verified 44,210,863-byte locked source archive through public `tarfile` read and write APIs. All 40 timed rewrite attempts across runs b and c read 6,539 members and 6,031 regular files, checked 136,064,031 regular-file bytes and source digest `4274d870abacbefea6bbdb2175de8b4073198e1fd39f34ede1c065b3faf1a805`, and wrote the identical 147,763,200-byte TAR with SHA-256 `8026bc5b211c69b1d7cda5a929b506cb7bf61d70290a3dfa5e51d275c4f85efd`. Both arms used an empty `PYTHONPYCACHEPREFIX` with bytecode writes disabled. Separate verbose imports named the source `tarfile.py`; the cache prefix remained empty after all attempts.

| Run and comparison | Paired wall changes, guard vs pure | Median paired wall | Median paired user + system CPU | Median peak RSS, pure / guard |
| --- | --- | ---: | ---: | ---: |
| b: 3 pure/pure pairs | −8.21%, −1.61%, −2.69% | −2.69% | −1.38% | 43.22 / 43.27 MB |
| b: 5 counterbalanced pairs | −12.04%, −10.09%, −9.61%, −11.35%, −9.69% | −10.09% | −10.65% | 43.30 / 43.52 MB |
| c: 3 pure/pure pairs | −0.71%, −5.82%, −1.04% | −1.04% | −0.85% | 43.30 / 43.27 MB |
| c: 5 counterbalanced pairs | −11.56%, −7.32%, −8.78%, −8.81%, −12.07% | −8.81% | −10.53% | 43.35 / 43.55 MB |

In run c, the comparison medians were 0.916/0.836 s wall, 0.766/0.672 s kernel user, and 0.131/0.133 s kernel system for pure/guard. Two further serial pairs per run formed a separate memory pass: guarded peak RSS was 0.18–0.26 MB higher in c and 0.36–1.34 MB higher in b. RSS is a lifetime process high-water mark, not unique or proportional memory; this evidence cannot establish memory parity. Run c's direct process swap counts were zero in every build, audit, probe, and rewrite attempt. Host swap allocation remained 243.88 MiB before and after both complete runs.

The first attempt, [run a data](data/tar-guard-measure-20260925-a.json), preserved three successful compiler records and one successful control source-import audit, then stopped before timing. Its audit parser falsely treated a `code object from` line for another module as evidence of a TAR `.pyc`; the verbose log actually showed `tarfile.py` source loading. The parser was narrowed, and run b used a distinct ID and scratch path. [Run b data](data/tar-guard-measure-20260925-b.json) preserves its 23 audit/probe/rewrite attempts and all pair orders, CPU, wall, and RSS fields. Its `wait4` records omitted per-process swaps, which prompted the distinct run c; no prior record was overwritten. No test suite, formatter, linter, hook, full PGO build, manifest edit, or patch edit was performed.
