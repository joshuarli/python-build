# TAR checksum pre-import helper guard, 2026-09-25

**Keep the optional guard.** `calc_chksums` now records whether its captured `struct.unpack_from` and `sum` are built-in callable types with the expected module and name. That decision is made once when `tarfile` imports. An ordinary Python function installed before import therefore takes the original two unpack/sum expressions; existing per-call identity checks handle replacements made afterward. The Rust path remains limited to exact built-in `bytes` of length 512, and `_rust_tar_checksum` still imports only on the first eligible call. Source inspection cannot prove identity against a deliberately spoofed C built-in callable or a custom importer. No semantic replacement probe was run.

Fresh verified extractions applied the default, TAR-only, and URL+TAR selections. Compared with the matching non-TAR selection, TAR changed its seven intended paths in both combinations. The native `build --variant tar-guard --tar-checksum` completed with locked LLVM 23.1.2, nightly Rust 2026-09-15, ThinLTO, and CPython's PGO task. Installed and patched-source `tarfile.py` both hash to `690d2b9b21fd2b97b07ab67bdcbfc2fdbbf7d5e1bee7bb430818f6a1a7e65e84`; the private extension exports its init and scanner symbols. Build resources: 296.26 s wall, 716.20 s user + 111.78 s system CPU, 1,824,800,768 B peak resident, 35,766,776 B peak footprint, and zero reported swaps.

Five alternating pairs per comparison read all 6,539 members and 6,031 regular files of the locked source TAR three times per process. All complete reads produced digest `4274d870abacbefea6bbdb2175de8b4073198e1fd39f34ede1c065b3faf1a805` over 408,192,093 logical bytes. Against the previous TAR-owned stage, the guard's median paired external wall change was **+0.90%** (range −0.27% to +1.48%); three control/control pairs drifted up to 0.97%. Median user CPU was 0.99 s versus 0.98 s. Against the quote-only stage on the same host, the guarded candidate retained a **9.01% median paired wall gain** (range 8.52% to 10.46% faster) and used 0.99 s versus 1.09 s median user CPU. Peak resident and footprint comparisons varied in direction; all attempts reported zero swaps.

Five paired cold imports showed a +0.97% median wall change, with individual changes from −3.48% to +7.11%; this does not establish an import cost. `/usr/bin/time -l -p` captured kernel CPU and lifetime peak memory, and an external clock captured wall time. OrbStack Docker activity continued on the host, while host swap allocation stayed at 243.88 MiB. Separate PGO builds also limit attribution of sub-percent differences. The first 20 timed attempts are retained as discarded calibration: a parser mismatch left their wall/output fields intact but lost their CPU and memory counters. The corrected runs contain 26 complete baseline/guard attempts and 10 quote-only/guard attempts in the [compact data](data/tar-helper-guard-20260925.json); [the measurement script](tar_helper_guard_measure.py) records the command, environment, pair order, and output checks. No separate test suite, formatter, linter, or hook ran.
