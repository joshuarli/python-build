# Third-party license evidence

The isolated Rust coverage overlay uses this crate with its exact version and
checksum pinned in `rust-cpython/overlay/Cargo.lock`:

| Crate | Version | Declared license |
| --- | --- | --- |
| `clap_lex` | 1.1.1 | MIT OR Apache-2.0 |

Both declared licenses are compatible with this lane. `clap_lex` has no
runtime dependencies.
