diff --git a/Lib/tarfile.py b/Lib/tarfile.py
--- a/Lib/tarfile.py
+++ b/Lib/tarfile.py
@@ -45,4 +45,5 @@
 from builtins import open as bltn_open
+import builtins
 import sys
 import os
 import io
@@ -218,6 +219,72 @@
 
     return s
 
+# Keep the Python expressions when a binding they would observe changes.
+# The native function checks also reject ordinary replacements before import.
+_ORIGINAL_UNPACK_FROM = struct.unpack_from
+_ORIGINAL_STRUCT = struct
+_ORIGINAL_SUM = sum
+_ORIGINAL_TYPE = type
+_ORIGINAL_BYTES = bytes
+_ORIGINAL_LEN = len
+_ORIGINAL_IMPORT = builtins.__import__
+_ORIGINAL_BUILTINS = builtins
+_ORIGINAL_SYS = sys
+_ORIGINAL_MODULES = sys.modules
+_ORIGINAL_META_PATH = sys.meta_path
+_ORIGINAL_META_FINDERS = tuple(sys.meta_path)
+_NATIVE_FUNCTION_TYPE = [].append.__class__
+_CHECKSUM_HELPERS_NATIVE = (
+    type is (1).__class__.__class__ and
+    bytes is b"".__class__ and
+    type(_ORIGINAL_LEN) is _NATIVE_FUNCTION_TYPE and
+    _ORIGINAL_LEN.__module__ == "builtins" and
+    _ORIGINAL_LEN.__name__ == "len" and
+    type(_ORIGINAL_IMPORT) is _NATIVE_FUNCTION_TYPE and
+    _ORIGINAL_IMPORT.__module__ == "builtins" and
+    _ORIGINAL_IMPORT.__name__ == "__import__" and
+    type(_ORIGINAL_UNPACK_FROM) is _NATIVE_FUNCTION_TYPE and
+    _ORIGINAL_UNPACK_FROM.__module__ == "_struct" and
+    _ORIGINAL_UNPACK_FROM.__name__ == "unpack_from" and
+    type(_ORIGINAL_SUM) is _NATIVE_FUNCTION_TYPE and
+    _ORIGINAL_SUM.__module__ == "builtins" and
+    _ORIGINAL_SUM.__name__ == "sum" and
+    type(sys.modules) is {}.__class__
+)
+_RUST_TAR_MODULE = None
+_RUST_TAR_SCAN = None
+
+def _unchanged_import_path():
+    if (sys.meta_path is not _ORIGINAL_META_PATH or
+            type(sys.meta_path) is not list or
+            len(sys.meta_path) != len(_ORIGINAL_META_FINDERS)):
+        return False
+    index = 0
+    while index < len(_ORIGINAL_META_FINDERS):
+        if sys.meta_path[index] is not _ORIGINAL_META_FINDERS[index]:
+            return False
+        index += 1
+    return True
+
+def _trusted_checksum_scan(buf):
+    global _RUST_TAR_MODULE, _RUST_TAR_SCAN
+    module = sys.modules.get("_rust_tar_checksum")
+    if _RUST_TAR_MODULE is None:
+        # An application-supplied private module must not set the result.
+        if "_rust_tar_checksum" in sys.modules:
+            return None
+        from _rust_tar_checksum import scan
+        module = sys.modules.get("_rust_tar_checksum")
+        if (type(module) is not type(sys) or
+                type(scan) is not _NATIVE_FUNCTION_TYPE or
+                scan.__self__ is not module):
+            return None
+        _RUST_TAR_MODULE = module
+        _RUST_TAR_SCAN = scan
+    if module is _RUST_TAR_MODULE:
+        return _RUST_TAR_SCAN(buf)
+    return None
+
 def calc_chksums(buf):
     """Calculate the checksum for a member's header by summing up all
        characters except for the chksum field which is treated as if
@@ -227,6 +294,18 @@
        the high bit set. So we calculate two checksums, unsigned and
        signed.
     """
+    if (_CHECKSUM_HELPERS_NATIVE and
+            type is _ORIGINAL_TYPE and bytes is _ORIGINAL_BYTES and
+            len is _ORIGINAL_LEN and builtins is _ORIGINAL_BUILTINS and
+            sys is _ORIGINAL_SYS and sys.modules is _ORIGINAL_MODULES and
+            builtins.__import__ is _ORIGINAL_IMPORT and
+            type(buf) is bytes and len(buf) == 512 and
+            struct is _ORIGINAL_STRUCT and
+            struct.unpack_from is _ORIGINAL_UNPACK_FROM and
+            sum is _ORIGINAL_SUM and _unchanged_import_path()):
+        checksums = _trusted_checksum_scan(buf)
+        if checksums is not None:
+            return checksums
     unsigned_chksum = 256 + sum(struct.unpack_from("148B8x356B", buf))
     signed_chksum = 256 + sum(struct.unpack_from("148b8x356b", buf))
     return unsigned_chksum, signed_chksum
diff --git a/Makefile.pre.in b/Makefile.pre.in
--- a/Makefile.pre.in
+++ b/Makefile.pre.in
@@ -3471,6 +3471,15 @@
 	mkdir -p Modules/_rust_url_quote
 	rustup run nightly-2026-09-15 rustc --edition=2024 --crate-type=staticlib -C opt-level=3 -C panic=abort $(if $(CARGO_TARGET),--target=$(CARGO_TARGET)) $< -o $@
 
+# Private no_std TAR header scanner, linked only into its own extension.
+RUST_TAR_CHECKSUM_A=Modules/_rust_tar_checksum/libtar_checksum.a
+MODULE__RUST_TAR_CHECKSUM_LDEPS=$(RUST_TAR_CHECKSUM_A)
+MODULE__RUST_TAR_CHECKSUM_LDFLAGS=$(RUST_TAR_CHECKSUM_A)
+
+$(RUST_TAR_CHECKSUM_A): $(srcdir)/Modules/_rust_tar_checksum/checksum.rs
+	mkdir -p Modules/_rust_tar_checksum
+	rustup run nightly-2026-09-15 rustc --edition=2024 --crate-type=staticlib -C opt-level=3 -C panic=abort $(if $(CARGO_TARGET),--target=$(CARGO_TARGET)) $< -o $@
+
 RUST_STATICLIB_A= target/$(if $(CARGO_TARGET),$(CARGO_TARGET)/$(CARGO_TARGET_DIR),$(CARGO_TARGET_DIR))/libcpython_rust_staticlib.a
 
 cpython-rust-staticlib: cpython-sys
diff --git a/Modules/Setup.stdlib.in b/Modules/Setup.stdlib.in
--- a/Modules/Setup.stdlib.in
+++ b/Modules/Setup.stdlib.in
@@ -120,6 +120,7 @@
 #
 @MODULE__BASE64_TRUE@_base64 _base64/Cargo.toml _base64/src/lib.rs
 @MODULE__RUST_URL_QUOTE_TRUE@_rust_url_quote _rust_url_quote/module.c
+@MODULE__RUST_TAR_CHECKSUM_TRUE@_rust_tar_checksum _rust_tar_checksum/module.c
 
 ############################################################################
 # Modules with some UNIX dependencies
diff --git a/configure.ac b/configure.ac
--- a/configure.ac
+++ b/configure.ac
@@ -7578,6 +7578,7 @@
   Modules/_testlimitedcapi \
   Modules/_xxtestfuzz \
   Modules/_rust_url_quote \
+  Modules/_rust_tar_checksum \
   Modules/_zstd \
   Modules/cjkcodecs \
   Modules/expat \
@@ -8617,6 +8618,9 @@
 PY_STDLIB_MOD([_rust_url_quote],
   [test "$HAVE_CARGO" = yes], [test "$HAVE_CARGO" = yes],
   [], [])
+PY_STDLIB_MOD([_rust_tar_checksum],
+  [test "$HAVE_CARGO" = yes], [test "$HAVE_CARGO" = yes],
+  [], [])
 
 dnl compression libs
 PY_STDLIB_MOD([zlib], [], [test "$have_zlib" = yes],
diff --git a/configure b/configure
--- a/configure
+++ b/configure
@@ -696,6 +696,8 @@
 MODULE__BASE64_TRUE
 MODULE__RUST_URL_QUOTE_FALSE
 MODULE__RUST_URL_QUOTE_TRUE
+MODULE__RUST_TAR_CHECKSUM_FALSE
+MODULE__RUST_TAR_CHECKSUM_TRUE
 MODULE__UUID_FALSE
 MODULE__UUID_TRUE
 MODULE__TKINTER_FALSE
@@ -32096,6 +32098,7 @@
   Modules/_testlimitedcapi \
   Modules/_xxtestfuzz \
   Modules/_rust_url_quote \
+  Modules/_rust_tar_checksum \
   Modules/_zstd \
   Modules/cjkcodecs \
   Modules/expat \
@@ -35766,6 +35769,45 @@
 
   { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $py_cv_module__rust_url_quote" >&5
 printf "%s\n" "$py_cv_module__rust_url_quote" >&6; }
+
+  { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for stdlib extension module _rust_tar_checksum" >&5
+printf %s "checking for stdlib extension module _rust_tar_checksum... " >&6; }
+        if test "$py_cv_module__rust_tar_checksum" != "n/a"
+then :
+
+    if test "$HAVE_CARGO" = yes
+then :
+  if test "$HAVE_CARGO" = yes
+then :
+  py_cv_module__rust_tar_checksum=yes
+else case e in #(
+  e) py_cv_module__rust_tar_checksum=missing ;;
+esac
+fi
+else case e in #(
+  e) py_cv_module__rust_tar_checksum=disabled ;;
+esac
+fi
+
+fi
+  as_fn_append MODULE_BLOCK "MODULE__RUST_TAR_CHECKSUM_STATE=$py_cv_module__rust_tar_checksum$as_nl"
+  if test "x$py_cv_module__rust_tar_checksum" = xyes
+then :
+
+
+
+
+fi
+   if test "$py_cv_module__rust_tar_checksum" = yes; then
+  MODULE__RUST_TAR_CHECKSUM_TRUE=
+  MODULE__RUST_TAR_CHECKSUM_FALSE='#'
+else
+  MODULE__RUST_TAR_CHECKSUM_TRUE='#'
+  MODULE__RUST_TAR_CHECKSUM_FALSE=
+fi
+
+  { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $py_cv_module__rust_tar_checksum" >&5
+printf "%s\n" "$py_cv_module__rust_tar_checksum" >&6; }
 
 
 
@@ -37078,6 +37120,10 @@
 fi
 if test -z "${MODULE__RUST_URL_QUOTE_TRUE}" && test -z "${MODULE__RUST_URL_QUOTE_FALSE}"; then
   as_fn_error $? "conditional \"MODULE__RUST_URL_QUOTE\" was never defined.
+Usually this means the macro was only invoked conditionally." "$LINENO" 5
+fi
+if test -z "${MODULE__RUST_TAR_CHECKSUM_TRUE}" && test -z "${MODULE__RUST_TAR_CHECKSUM_FALSE}"; then
+  as_fn_error $? "conditional \"MODULE__RUST_TAR_CHECKSUM\" was never defined.
 Usually this means the macro was only invoked conditionally." "$LINENO" 5
 fi
 if test -z "${MODULE_ZLIB_TRUE}" && test -z "${MODULE_ZLIB_FALSE}"; then
diff --git a/Modules/_rust_tar_checksum/module.c b/Modules/_rust_tar_checksum/module.c
new file mode 100644
--- /dev/null
+++ b/Modules/_rust_tar_checksum/module.c
@@ -0,0 +1,42 @@
+/* CPython owns the exact TAR header while Rust scans its 512 bytes. */
+#define PY_SSIZE_T_CLEAN
+#include <Python.h>
+
+extern void tar_checksum_scan(const unsigned char *, int *, int *);
+
+static PyObject *scan(PyObject *self, PyObject *arg)
+{
+    int unsigned_sum;
+    int signed_sum;
+    (void)self;
+    if (!PyBytes_CheckExact(arg) || PyBytes_GET_SIZE(arg) != 512) {
+        PyErr_SetString(PyExc_TypeError, "scan requires exact bytes of length 512");
+        return NULL;
+    }
+    tar_checksum_scan((const unsigned char *)PyBytes_AS_STRING(arg),
+                      &unsigned_sum, &signed_sum);
+    return Py_BuildValue("(ii)", unsigned_sum, signed_sum);
+}
+
+static PyMethodDef methods[] = {
+    {"scan", scan, METH_O, "Return unsigned and signed checksums of an exact TAR header."},
+    {NULL, NULL, 0, NULL}
+};
+
+static PyModuleDef_Slot slots[] = {
+    {Py_mod_multiple_interpreters, Py_MOD_PER_INTERPRETER_GIL_SUPPORTED},
+    {0, NULL}
+};
+
+static struct PyModuleDef module = {
+    PyModuleDef_HEAD_INIT,
+    .m_name = "_rust_tar_checksum",
+    .m_size = 0,
+    .m_methods = methods,
+    .m_slots = slots,
+};
+
+PyMODINIT_FUNC PyInit__rust_tar_checksum(void)
+{
+    return PyModuleDef_Init(&module);
+}
diff --git a/Modules/_rust_tar_checksum/checksum.rs b/Modules/_rust_tar_checksum/checksum.rs
new file mode 100644
--- /dev/null
+++ b/Modules/_rust_tar_checksum/checksum.rs
@@ -0,0 +1,37 @@
+//! Scan one exact TAR header while CPython retains its bytes object.
+#![no_std]
+
+use core::panic::PanicInfo;
+
+unsafe extern "C" {
+    fn abort() -> !;
+}
+
+#[panic_handler]
+fn panic(_info: &PanicInfo<'_>) -> ! {
+    unsafe { abort() }
+}
+
+// The C caller supplies a live 512-byte header and two writable C integers.
+#[unsafe(no_mangle)]
+pub unsafe extern "C" fn tar_checksum_scan(
+    buf: *const u8,
+    unsigned: *mut i32,
+    signed: *mut i32,
+) {
+    let mut u = 256i32;
+    let mut s = 256i32;
+    let mut offset = 0;
+    while offset < 512 {
+        if offset < 148 || offset >= 156 {
+            let byte = unsafe { *buf.add(offset) };
+            u += i32::from(byte);
+            s += i32::from(byte as i8);
+        }
+        offset += 1;
+    }
+    unsafe {
+        *unsigned = u;
+        *signed = s;
+    }
+}
