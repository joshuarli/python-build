diff --git a/Modules/zlibmodule.c b/Modules/zlibmodule.c
--- a/Modules/zlibmodule.c
+++ b/Modules/zlibmodule.c
@@ -29,6 +29,16 @@ extern int python_build_rs_inflateSetDictionary(z_streamp, const Bytef *, uInt);
 #define inflateSetDictionary python_build_rs_inflateSetDictionary
 #endif
 
+#if defined(PYTHON_BUILD_ZLIB_HYBRID) && defined(PYTHON_BUILD_ZLIB_ONESHOT)
+#error "select one zlib inflate backend mode"
+#endif
+
+#ifdef PYTHON_BUILD_ZLIB_ONESHOT
+extern int python_build_rs_inflateInit2_(z_streamp, int, const char *, int);
+extern int python_build_rs_inflate(z_streamp, int);
+extern int python_build_rs_inflateEnd(z_streamp);
+#endif
+
 #if defined(ZLIB_VERNUM) && ZLIB_VERNUM < 0x1221
 #error "At least zlib version 1.2.2.1 is required"
 #endif
@@ -437,6 +447,13 @@ zlib.decompress
 Returns a bytes object containing the uncompressed data.
 [clinic start generated code]*/
 
+/* The local stream must use one backend from initialization through end. */
+#ifdef PYTHON_BUILD_ZLIB_ONESHOT
+#define inflateInit2_ python_build_rs_inflateInit2_
+#define inflate python_build_rs_inflate
+#define inflateEnd python_build_rs_inflateEnd
+#endif
+
 static PyObject *
 zlib_decompress_impl(PyObject *module, Py_buffer *data, int wbits,
                      Py_ssize_t bufsize)
@@ -544,6 +561,12 @@ zlib_decompress_impl(PyObject *module, Py_buffer *data, int wbits,
     return NULL;
 }
 
+#ifdef PYTHON_BUILD_ZLIB_ONESHOT
+#undef inflateInit2_
+#undef inflate
+#undef inflateEnd
+#endif
+
 /*[clinic input]
 zlib.compressobj
 
